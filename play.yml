---
- hosts: localhost
  gather_facts: False
  roles:
    - role: ansible-rclone

  tasks:
  
  - name: Install required packages
    apt: name={{item}} state=installed
    with_items:
      - unrar
      - unzip
      - p7zip
      - python-pip
      - python3-pip
      - docker.io