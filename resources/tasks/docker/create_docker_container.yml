##################################################################################
# Title:         Cloudbox: Resources | Tasks | Docker | Create Docker Container  #
# Author(s):     desimaniac                                                      #
# URL:           https://github.com/cloudbox/cloudbox                            #
# --                                                                             #
#         Part of the Cloudbox project: https://cloudbox.works                   #
##################################################################################
#                   GNU General Public License v3.0                              #
##################################################################################
---
- name: Resources | Tasks | Docker | Create Docker Container
  docker_container:
    name: "{{ lookup('vars', role_name + '_docker_container') }}"
    image: "{{ lookup('vars', role_name + '_docker_image') }}"
    pull: "{{ lookup('vars', role_name + '_docker_image_pull') }}"
    published_ports: "{{ lookup('vars', role_name + '_docker_ports') }}"
    env: "{{ lookup('vars', role_name + '_docker_envs') }}"
    volumes: "{{ lookup('vars', role_name + '_docker_volumes') }}"
    devices: "{{ lookup('vars', role_name + '_docker_devices') }}"
    etc_hosts: "{{ lookup('vars', role_name + '_docker_hosts') }}"
    labels: "{{ lookup('vars', role_name + '_docker_labels') }}"
    hostname: "{{ lookup('vars', role_name + '_docker_hostname') }}"
    networks: "{{ lookup('vars', role_name + '_docker_networks') }}"
    networks_cli_compatible: yes
    restart_policy: "{{ lookup('vars', role_name + '_docker_restart_policy') }}"
    capabilities: "{{ lookup('vars', role_name + '_docker_capabilities') }}"
    security_opts: "{{ lookup('vars', role_name + '_docker_security_opts') }}"
    stop_timeout: "{{ lookup('vars', role_name + '_docker_stop_timeout', default='180') }}"
    state: "{{ lookup('vars', role_name + '_docker_state') }}"
